[project]
name = "dbt-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "dbt-core>=1.10.22",
    "dbt-snowflake>=1.10.2",
    "sqlfluff>=4.1.0",
]
